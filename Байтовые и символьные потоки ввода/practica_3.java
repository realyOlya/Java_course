Вот основные причины, зачем нужны отдельные иерархии:

1. Поддержка кодировок (Encoding)
Байтовые потоки просто передают «сырые» данные. Они не знают, что байт 0x41 — это латинская 'A',
 а последовательность из двух байтов может быть одним русским символом 'Я' в UTF-8.

Байтовые потоки считывают ровно столько байтов, сколько их в файле.

Символьные потоки используют таблицу кодировки, чтобы правильно собрать байты в понятные Java-символы (char).
 Без них работа с кириллицей, китайскими иероглифами или эмодзи превратилась бы в кошмар с «битыми» символами.


 2.Размер единицы данных
   InputStream/OutputStream: Базовая единица — byte (8 бит). Это идеально для картинок, аудио, видео и любых бинарных файлов.

   Reader/Writer: Базовая единица — char (16 бит, стандарт UTF-16 в Java). Это создано специально для текстовых документов.



3. Интернационализация (i18n)
Иерархия Reader/Writer была добавлена в Java 1.1, когда стало понятно, что байтовых потоков недостаточно
для корректной работы во всем мире. Она позволяет программе автоматически адаптироваться к локальной кодировке системы,
 не заставляя программиста вручную пересчитывать байты в символы.